#include<iostream>
#include "arr_functions.h"

using std::cout;

//define iterate_array and loop through with ++ increment
void iterateArray(int* hours, const int SIZE) {
	for (int = o; i < SIZE; ++i) {
		cout << hours[i] << "\n";
	}
}